# AI-Powered-Zoho-Project-Chatbot
This is a conversational AI chatbot that connects to Zoho Projects via its REST API. Users log in via their  own Zoho OAuth credentials, and interact through a chat UI using natural language. The backend is a multi- agent LangGraph system running on FastAPI — one agent handles queries, another handles actions. This
